import { styled } from '../stitches.config';

export const ContentContainer = styled('div', {
  maxWidth: 1310,
  paddingRight: 80,
  paddingLeft: 80,
  width: '100%',
  marginLeft: 'auto',
  marginRight: 'auto',
  overflow: 'visible',
});
